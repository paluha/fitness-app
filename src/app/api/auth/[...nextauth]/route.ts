import { NextResponse } from 'next/server';

// Auth placeholder - not configured yet
export async function GET() {
  return NextResponse.json({ message: 'Auth not configured' });
}

export async function POST() {
  return NextResponse.json({ message: 'Auth not configured' });
}
